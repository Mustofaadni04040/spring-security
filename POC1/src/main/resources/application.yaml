spring:
  application:
    name: POC1
  datasource:
    url: jdbc:mysql://localhost:3306/scurityDemo
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.hibernate.format_sql: true